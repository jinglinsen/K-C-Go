# K-C-go Backend Requirements - Optimized for Docker (Python 3.11+)
# Only includes primary dependencies. Sub-dependencies are resolved automatically.

# Framework & Server
fastapi>=0.115.0
uvicorn
gunicorn

# Data & Database
sqlalchemy>=2.0.0
pydantic-settings
psycopg2-binary   # PostgreSQL driver (pre-compiled, no gcc needed)

# Security
pyjwt             # For JWT Token
passlib[bcrypt]    # For Secure Password Hashing
python-multipart  # For Form/File uploads

# Utilities
python-dotenv
